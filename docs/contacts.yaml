openapi: 3.0.3
info:
  title: NeetoCRM Contacts APIs
  version: 1.0.0
  description: APIs for managing contacts in your NeetoCRM workspace.
  contact:
    email: support@neetocrm.com

servers:
  $ref: '../schemas/common.yaml#/servers'

tags:
  - name: Contacts
    description: APIs to manage contacts in your workspace.

paths:
  /contacts:
    get:
      summary: List all contacts
      tags:
        - Contacts
      description: Retrieve a list of all contacts in your workspace. Results are ordered by creation date (newest first).
      parameters:
        - $ref: '../schemas/common.yaml#/api_key_header'
        - $ref: '../schemas/common.yaml#/page_number_param'
        - $ref: '../schemas/common.yaml#/page_size_param'
      responses:
        '200':
          description: Successfully retrieved contacts.
          content:
            application/json:
              schema:
                $ref: '../schemas/contact.yaml#/components/schemas/contact_list_response'
    post:
      summary: Create a contact
      tags:
        - Contacts
      description: Create a new contact in your workspace.
      parameters:
        - $ref: '../schemas/common.yaml#/api_key_header'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '../schemas/contact.yaml#/components/schemas/contact_request'
      responses:
        '201':
          description: Contact created successfully.
          content:
            application/json:
              schema:
                $ref: '../schemas/contact.yaml#/components/schemas/contact_response'

  /contacts/{id}:
    get:
      summary: Get a contact
      tags:
        - Contacts
      description: Retrieve a single contact by its ID.
      parameters:
        - $ref: '../schemas/common.yaml#/api_key_header'
        - name: id
          in: path
          required: true
          description: The ID of the contact to retrieve. You can get the contact `id` by listing all contacts using our [List all contacts](/api-reference/contacts/list-contacts) API.
          schema:
            type: string
      responses:
        '200':
          description: Successfully retrieved the contact.
          content:
            application/json:
              schema:
                $ref: '../schemas/contact.yaml#/components/schemas/contact_response'
    patch:
      summary: Update a contact
      tags:
        - Contacts
      description: Update an existing contact by its ID.
      parameters:
        - $ref: '../schemas/common.yaml#/api_key_header'
        - name: id
          in: path
          required: true
          description: The ID of the contact to update. You can get the contact `id` by listing all contacts using our [List all contacts](/api-reference/contacts/list-contacts) API.
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '../schemas/contact.yaml#/components/schemas/contact_request'
      responses:
        '200':
          description: Contact updated successfully.
          content:
            application/json:
              schema:
                $ref: '../schemas/contact.yaml#/components/schemas/contact_response'
    delete:
      summary: Delete a contact
      tags:
        - Contacts
      description: Delete a contact by its ID.
      parameters:
        - $ref: '../schemas/common.yaml#/api_key_header'
        - name: id
          in: path
          required: true
          description: The ID of the contact to delete. You can get the contact `id` by listing all contacts using our [List all contacts](/api-reference/contacts/list-contacts) API.
          schema:
            type: string
      responses:
        '200':
          description: Contact deleted successfully.
          content:
            application/json:
              schema:
                $ref: '../schemas/contact.yaml#/components/schemas/contact_delete_response'
