---
title: "Introduction"
description: "Welcome to the NeetoCRM API documentation."
---

The NeetoCRM API is organized around [REST](https://en.wikipedia.org/wiki/REST) principles and uses standard [HTTP](https://en.wikipedia.org/wiki/HTTP) response codes. All API endpoints return [JSON](https://en.wikipedia.org/wiki/JSON) responses and require authentication.

Before you start with NeetoCRM APIs you will need the following:

1. [Workspace subdomain](/getting-started/workspace-subdomain).
2. [API key](/getting-started/authentication).

## Making your first API call

Let's retrieve all contacts in your workspace using a [GET request](https://en.wikipedia.org/wiki/HTTP#Request_methods):

<CodeGroup>
```bash curl
curl -X GET "https://{your-subdomain}.neetocrm.com/api/external/v1/contacts" \
  -H 'X-Api-Key: your-api-key' \
  -H 'Content-Type: application/json'
```

```javascript JavaScript
const response = await fetch(
  "https://{your-subdomain}.neetocrm.com/api/external/v1/contacts",
  {
    method: "GET",
    headers: {
      "X-Api-Key": "your-api-key",
      "Content-Type": "application/json",
    },
  }
);

const data = await response.json();
console.log(data);
```

```python Python
import requests

url = "https://{your-subdomain}.neetocrm.com/api/external/v1/contacts"
headers = {
    "X-Api-Key": "your-api-key",
    "Content-Type": "application/json"
}

response = requests.get(url, headers=headers)
data = response.json()
print(data)
```

</CodeGroup>

<Warning>
  Replace `your-subdomain` with your actual workspace subdomain and
  `your-api-key` with your actual API key.
</Warning>

## Rate Limiting

API requests are subject to rate limiting to ensure fair use. If you exceed the rate limit, you'll receive a 429 response. If you encounter this error try again after some time.

<Card title='Need help?'>
  Visit our [help center](https://help.neetocrm.com/) or contact us by sending
  an email to [support@neetocrm.com](mailto:support@neetocrm.com).
</Card>
